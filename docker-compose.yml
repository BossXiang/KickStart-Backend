version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '6000:6000'
    environment:
      - MYSQL_USERNAME='tom'
      - MYSQL_PASSWORD='123456789'
      - MYSQL_DATABASE='sql_test'